# cli-timer
A CLI timer that supports commands while it runs.

# Usage
This timer supports commands like:
    - 'p': pause in the middle of a session
    - 's': start again (if paused)

It stores each session with details on a file `sessions/<DATE>`

